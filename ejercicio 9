#ejercicio 9
temperatura_celsius = float(input("ingrese temperatura en grados celsius: "))

fahrenheit = (temperatura_celsius * 9/5 ) + 32

print(f"temperatura celsius: {temperatura_celsius}")
print(f"temperatura fahrenheit: {fahrenheit}")
